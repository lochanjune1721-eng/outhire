# Vercel project environment variables.
#
# THIS FILE IS A COMMITTED TEMPLATE IN A PUBLIC REPOSITORY.
# Never put a real value in it. Real values go in the Vercel dashboard
# (Project -> Settings -> Environment Variables) and nowhere else in this repo.
#
# The one exception is SUPABASE_ANON_KEY, which is public by design and is read
# from js/config.js because there is no build step to inject it.

SUPABASE_URL=
SUPABASE_ANON_KEY=              # safe in client JS (also lives in js/config.js)
SUPABASE_SERVICE_ROLE_KEY=      # serverless + seed script only, never client

# Dodo Payments
DODO_API_KEY=
DODO_WEBHOOK_SECRET=            # the whsec_... signing secret
DODO_PRODUCT_ID=                # the credit product created in the Dodo dashboard
DODO_MODE=test                  # test | live
# DODO_API_BASE=                # override only if Dodo's host differs

ADMIN_PASSWORD=

SITE_URL=
